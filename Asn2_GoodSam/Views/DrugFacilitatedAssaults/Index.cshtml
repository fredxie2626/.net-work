@model IEnumerable<Asn2_GoodSam.Models.Client.DrugFacilitatedAssault>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DrugFacilitatedAssaultName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DrugFacilitatedAssaultName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugFacilitatedAssaultId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugFacilitatedAssaultId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugFacilitatedAssaultId })
        </td>
    </tr>
}

</table>
