@model IEnumerable<Asn2_GoodSam.Models.Client.MedicalOnly>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MedicalOnlyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MedicalOnlyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MedicalOnlyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MedicalOnlyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MedicalOnlyId })
        </td>
    </tr>
}

</table>
