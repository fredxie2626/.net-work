@model IEnumerable<Asn2_GoodSam.Models.Client.AbuserRelationship>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AbuserRelationshipName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AbuserRelationshipName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AbuserRelationshipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AbuserRelationshipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AbuserRelationshipId })
        </td>
    </tr>
}

</table>
