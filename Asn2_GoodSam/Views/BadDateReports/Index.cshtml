@model IEnumerable<Asn2_GoodSam.Models.Client.BadDateReport>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BadDateReportName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BadDateReportName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BadDateReportId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BadDateReportId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BadDateReportId })
        </td>
    </tr>
}

</table>
