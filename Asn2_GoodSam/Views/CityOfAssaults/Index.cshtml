@model IEnumerable<Asn2_GoodSam.Models.Client.CityOfAssault>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CityOfAssaultName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CityOfAssaultName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CityOfAssaultId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CityOfAssaultId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CityOfAssaultId })
        </td>
    </tr>
}

</table>
