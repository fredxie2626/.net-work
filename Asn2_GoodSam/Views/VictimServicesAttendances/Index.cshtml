@model IEnumerable<Asn2_GoodSam.Models.Client.VictimServicesAttendance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VictimServicesAttendanceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VictimServicesAttendanceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VictimServicesAttendanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VictimServicesAttendanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VictimServicesAttendanceId })
        </td>
    </tr>
}

</table>
