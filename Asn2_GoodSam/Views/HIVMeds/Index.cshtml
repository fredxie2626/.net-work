@model IEnumerable<Asn2_GoodSam.Models.Client.HIVMeds>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HIVMedsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HIVMedsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HIVMedsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HIVMedsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HIVMedsId })
        </td>
    </tr>
}

</table>
