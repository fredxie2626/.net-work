@model IEnumerable<Asn2_GoodSam.Models.Client.RiskStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RiskStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RiskStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RiskStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RiskStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RiskStatusId })
        </td>
    </tr>
}

</table>
