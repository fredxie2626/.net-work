@model IEnumerable<Asn2_GoodSam.Models.Client.ReferredToCBVS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReferredToCBVSName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReferredToCBVSName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferredToCBVSId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferredToCBVSId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferredToCBVSId })
        </td>
    </tr>
}

</table>
