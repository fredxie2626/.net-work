@model IEnumerable<Asn2_GoodSam.Models.Client.ReferralContact>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReferralContactName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReferralContactName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReferralContactId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReferralContactId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReferralContactId })
        </td>
    </tr>
}

</table>
