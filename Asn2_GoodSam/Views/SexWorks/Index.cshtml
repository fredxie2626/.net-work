@model IEnumerable<Asn2_GoodSam.Models.Client.SexWork>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SexWorkName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SexWorkName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SexWorkId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SexWorkId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SexWorkId })
        </td>
    </tr>
}

</table>
