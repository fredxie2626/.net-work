@model IEnumerable<Asn2_GoodSam.Models.Client.RepeatClient>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RepeatClientName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RepeatClientName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RepeatClientId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RepeatClientId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RepeatClientId })
        </td>
    </tr>
}

</table>
