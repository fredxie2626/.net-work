@model IEnumerable<Asn2_GoodSam.Models.Client.MultiplePerpetrators>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MultiplePerpetratorsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MultiplePerpetratorsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MultiplePerpetratorsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MultiplePerpetratorsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MultiplePerpetratorsId })
        </td>
    </tr>
}

</table>
