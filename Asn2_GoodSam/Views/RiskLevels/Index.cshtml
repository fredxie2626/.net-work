@model IEnumerable<Asn2_GoodSam.Models.Client.RiskLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RiskLevelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RiskLevelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RiskLevelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RiskLevelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RiskLevelId })
        </td>
    </tr>
}

</table>
